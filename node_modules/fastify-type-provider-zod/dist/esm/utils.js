//#region src/utils.ts
var assertIsOpenAPIObject = (obj) => {
	if ("swaggerObject" in obj) throw new Error("This package currently does not support component references for Swagger 2.0");
};
var getReferenceUri = (input) => {
	const id = input.replace(/^#\/(?:\$defs|definitions|components\/schemas)\//, "");
	if (id === "") throw new Error("Unable to build a component reference for an unnamed schema. Recursive or self-referential schemas used inline must be registered with an `id` so they can be referenced as components.");
	return `#/components/schemas/${id}`;
};
var getJSONSchemaTarget = (version = "3.0.0") => {
	if (version.startsWith("3.0")) return "openapi-3.0";
	return "draft-2020-12";
};
//#endregion
export { assertIsOpenAPIObject, getJSONSchemaTarget, getReferenceUri };

//# sourceMappingURL=utils.js.map